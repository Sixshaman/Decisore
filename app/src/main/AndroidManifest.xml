<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.sixshaman.advancedunforgetter">

    <application
            android:allowBackup="true"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".scheduler.ObjectiveSchedulerActivity"
                android:label="@string/title_activity_task_scheduler"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".scheduler.TaskChainsActivity"
                android:label="@string/title_activity_task_chains"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".scheduler.TaskPoolsActivity"
                android:label="@string/title_activity_task_pools"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".archive.ArchiveActivity">
        </activity>
        <activity
                android:name=".list.ListActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>