<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menuOpenArchive"
        android:title="@string/openArchive"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/menuOpenScheduler"
        android:title="@string/openScheduler"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/menuOpenOptions"
        android:title="@string/openOptions"
        app:showAsAction="never">
    </item>

</menu>